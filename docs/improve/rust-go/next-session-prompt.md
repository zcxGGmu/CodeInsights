# Rust / Go 优化重构下次启动提示词

> 更新时间：2026-06-07
> 当前分支：`rust-go-refactor`
> 最新已确认实现基线：`63e0fc2b feat(rust-go): 外显 no-go audit 并加固 install-chain manifest`。
> 最新已确认状态同步 / 恢复入口：`885e89b5 docs(rust-go): 同步 Phase 5 no-go audit summary 状态`；若本轮状态回填再次提交后，下次启动以 `git log -5 --oneline` 中最新的 Rust / Go docs 提交为实际恢复入口。

## 当前真实进度

已完成：

- [x] Rust / Go 优化重构方案：`docs/improve/rust-go/2026-06-01-rust-go-optimization-plan.md`。
- [x] Rust / Go 开发跟踪清单：`docs/improve/rust-go/2026-06-01-rust-go-development-checklist.md`。
- [x] 阶段提交纪律固化：`tasks/lessons.md` 已记录每完成一个阶段并通过验证后必须单独提交；重启 Codex 会话后也要主动检查是否有已完成但未提交的阶段成果。
- [x] Phase 0：已建立 `packages/shared` NativeRuntime DTO / IPC 草案、contract fixtures、主进程 `NativeRuntimeAdapter` TypeScript interface、diagnostics 空实现和 `native-runtime:benchmark` runner。
- [x] Phase 1：已新增 `jsonl-event-reader.ts`、`ts-event-search-service.ts` 和 `native-runtime-service.ts`，统一 Chat / Agent / Pipeline TypeScript 搜索 fallback facade，并保留旧 IPC / preload / renderer 行为兼容。
- [x] Phase 2：已新增 TypeScript Pipeline cursor tail service，支持 latest / before / after、cursor schema version、anchor 校验、截断安全回退、坏行/空行/部分写入容错，并接入 SearchDialog Pipeline 内容搜索和 record 聚焦。
- [x] Phase 3：已新增 TypeScript workspace 文件索引 cache，支持 root fingerprint、ignore 规则、entry 上限、in-flight build 复用、watcher invalidation、workspace / attached source metadata 和 main 端路径白名单。
- [x] Phase 4：已新增 Native Runtime diagnostics IPC / preload / Jotai atoms / 全局 listener / Settings 运行诊断面板，支持 status、diagnostics、rebuildIndex、clearCache、operation progress、status changed；SearchDialog 已具备 source filter、fallback / rebuilding / unavailable / error 简短状态和更多结果控制；PipelineRecords 已具备 tail loading / retry 状态。
- [x] Phase 4 验证：Native Runtime / IPC / atoms / hook / SearchDialog / Diagnostics / shared tests、Pipeline renderer tests、shared/electron typecheck、main/preload/renderer build、`bun install --frozen-lockfile --dry-run` 和 `git diff --check` 均通过；`build:renderer` 仅有既有大 chunk 警告。
- [x] Phase 5 前置计划：已在 `tasks/todo.md` 写清范围、触达文件、验证命令、性能收益门槛、依赖 decision record 和禁止事项。
- [x] Phase 5 前置依赖决策：已新增 `docs/improve/rust-go/2026-06-03-phase-5-dependency-decision-record.md`，结论是首个 Rust sidecar 只建议实现阶段评估 `serde`、`serde_json` 和可选 `memchr`；暂缓 `regex`、`walkdir`、`ignore`、`memmap2`、`tantivy`；Go / `fsnotify` 不进入 Phase 5 默认实现。
- [x] Phase 5 protocol / fallback / smoke 计划：已新增 `docs/improve/rust-go/2026-06-03-phase-5-sidecar-protocol-and-smoke-plan.md`，首版采用 stdin / stdout line-delimited JSON protocol，method 为 `status`、`search`、`tail_jsonl`、`shutdown`，并要求 native missing / available、protocol mismatch、crash、timeout、cache corruption 和“不使用系统 PATH” smoke。
- [x] Phase 5 TS fallback benchmark gate：已重新跑 100MB 级 benchmark；Chat JSONL 99,527,780 bytes，P95 279.622ms，event loop delay 10.020ms；Agent JSONL 102,397,780 bytes，P95 216.859ms，event loop delay 1.248ms，确认 search 路径仍有 Rust 试点收益空间。
- [x] Phase 5 最小 Rust search-only sidecar 源码切片：已新增 `native/search/`，包含 stdin / stdout line-delimited JSON protocol、`status`、literal `search`、`shutdown` 和 explicit out-of-scope `tail_jsonl` typed error；直接依赖固定为 `serde = 1.0.228`、`serde_json = 1.0.150`。
- [x] Phase 5 search-only Rust 单测：覆盖 status、literal search、bad JSON、missing source、empty query、limit clamp、long query、deadline、snippet cap、基础脱敏、中文命中、ASCII query + Unicode prefix offset 和 UTF-16 matchedRanges。
- [x] Phase 5 Electron main process sidecar manager 与 fallback gate：已新增 `native-runtime-sidecar-manager.ts`，覆盖 missing binary、version mismatch、timeout、crash、shutdown、bad JSON stdout、stdout buffer 上限、source boundary violation、range violation、contract violation 和拒绝类错误不 fallback；默认仍不启用 native。
- [x] Phase 5 Chat search native opt-in 边界：只有 `CODEINSIGHTS_NATIVE_RUNTIME=1`、`CODEINSIGHTS_NATIVE_SEARCH=1` 和 `CODEINSIGHTS_NATIVE_SEARCH_BINARY` 同时存在时才尝试 Rust sidecar；native 命中只用于定位 cursor，legacy snippet / matchedRanges 仍由 TypeScript 生成。
- [x] Phase 5 Rust vs TS contract parity 与 100MB native benchmark：真实 Rust sidecar parity 已通过；`319f30e8` 当时 benchmark 显示 Rust native Chat event loop delay 明显降低但 P95 未达标，Agent native 明显慢于 TS fallback，因此 native 不得默认启用。
- [x] Phase 5 Rust search 早停性能优化：`native/search` 已升级到 `0.0.2` / `0.0.2-dev`，sidecar 在第 `limit + 1` 个命中后停止扫描并返回 `hasMore=true`，不再为精确 total 完整解析 100MB JSONL；新增 Rust 单测锁住早停后不继续统计后续坏 JSON 行。100MB benchmark 已从 P95 不达标改善为：Rust native Chat P95 25.593ms、event loop delay 2.432ms；Rust native Agent P95 10.573ms、event loop delay 3.546ms。
- [x] Phase 5 稳定 benchmark gate 复跑：本轮先发现本地 release binary 仍报告 `0.0.1-dev`，已重建并确认 `0.0.2-dev` 后复跑 2 轮 100MB / 20 iterations benchmark。第 1 轮：TS Chat P95 277.973ms / event loop 62.511ms，Rust native Chat P95 3.516ms / event loop 1.651ms；TS Agent P95 227.239ms / event loop 1.269ms，Rust native Agent P95 2.203ms / event loop 1.732ms。第 2 轮：TS Chat P95 267.874ms / event loop 9.793ms，Rust native Chat P95 3.429ms / event loop 1.478ms；TS Agent P95 220.111ms / event loop 1.127ms，Rust native Agent P95 2.201ms / event loop 1.416ms。P95 gate 稳定达标，Chat event loop gate 达标；Agent event loop delay 两轮仍小幅高于 TS，因此 default enable 仍阻断。
- [x] Phase 5 benchmark event-loop 口径增强：旧 `eventLoopDelayMs` 已确认是每个 case 的 `setTimeout(0)` 最大 delay，不是 P95，也不是搜索内核独立阻塞时间；benchmark summary 现在额外输出 `eventLoopDelaySamplesMs`、`eventLoopDelayMaxMs`、`eventLoopDelayP95Ms`、`eventLoopBaselineMs`、`eventLoopBaselineP95Ms`、`eventLoopWorkDelayMs`、`eventLoopWorkDelayP95Ms`，并新增 `native-sidecar-status-cache-overhead` case。小规模 native smoke 已确认本地 `0.0.2-dev` release binary 输出新字段。
- [x] Phase 5 新 event-loop 字段稳定 benchmark 复跑：本轮先用 release sidecar `status` 确认本地 ignored binary 为 `0.0.2-dev`，未重建、未创建 packaged native binary。2 轮 100MB / 20 iterations 结果显示 native P95 继续稳定达标，Chat work delay gate 达标；但 Agent native `eventLoopWorkDelayP95Ms` 两轮仍小幅高于 TS fallback（0.065ms vs 0ms；0.164ms vs 0.047ms），未扣 baseline 的 Agent `eventLoopDelayP95Ms` 也两轮小幅回退（1.199ms vs 1.121ms；1.191ms vs 1.114ms）。`native-sidecar-status-cache-overhead` P95 为 0.014ms / 0.013ms。结论：Agent event-loop gate 仍不足以支持 default enable，native 继续显式 opt-in / default off。
- [x] Phase 5 native benchmark gate 汇总：`f86553ee feat(rust-go): 补齐 Phase 5 native benchmark gate 汇总` 已在 `native-runtime:benchmark` summary 中新增 `nativeSearchGate`，机器可读输出 Chat / Agent TS vs native 的 P95、event-loop delay P95、work-delay P95 delta、benchmark blockers 和 default-enable blockers。无 native binary 时仍保留 optional package / packaged app bundled binary 阻塞项；benchmark 全部通过时也不会把 `defaultEnableCandidate` 误置为 true。
- [x] Phase 5 Agent production facade benchmark 分析：`9a06908a feat(rust-go): 补齐 Phase 5 Agent facade benchmark 分析` 已新增 `agentFacadeSearch` summary 与 `agent-runtime-production-facade-search` case，使用 nested SDK message content fixture 和生产 Agent 搜索等价的文本提取逻辑，证明 direct native Agent benchmark 不能代表生产 Agent facade。
- [x] Phase 5 Agent nested content native parity：`665c5db7 feat(rust-go): 补齐 Phase 5 Agent nested native parity` 已新增受限白名单 extractor `agent_message_search_text`，Rust sidecar 只搜索顶层 legacy `content` 或 `message.content[]` 中 `type="text"` 的字符串 block，不搜索 raw JSON / `tool_use` / 任意 nested path；生产 Agent source 在显式 native opt-in 时可声明该 extractor，native 命中仍只作为 cursor anchor，legacy snippet / messageId 继续由 TypeScript 回读 JSONL 后重建。
- [x] Phase 5 基础 smoke script：已新增 `apps/electron/scripts/native-runtime-smoke.ts` 和 `smoke:native-runtime`，支持 `native-missing` 与 `native-available` 入口；未显式提供 binary 时 `native-available` 标记 skipped，不从系统 `PATH` 查找；`native-missing` 验证 TS fallback 可用和 `missing_binary` fallback。
- [x] Phase 5 native-cache schema helper：已新增 `native-runtime-cache-schema.ts`，cache root 固定为 `getConfigDir()/native-cache`，manifest 记录 schema / protocol / package plan，不记录 binary path；损坏 manifest 返回 `cache_corrupted`。
- [x] Phase 5 fake sidecar / cache-corruption smoke：`2cc95b1b feat(rust-go): 补齐 Phase 5 fake sidecar smoke 失败路径` 已把 `protocol-mismatch`、`crash`、`timeout`、`cache-corruption` smoke 从 skipped 改为可执行 fixture。前三者使用 `process.execPath + 临时 JS fake sidecar`，分别验证 `version_mismatch`、`crashed`、`timeout` fallback；`cache-corruption` 使用隔离 `CODEINSIGHTS_CONFIG_DIR/native-cache/manifest.json` 写入损坏 manifest，验证 `cache_corrupted`。四个 mode 均同时验证 TypeScript fallback 搜索可用、summary 脱敏、不从系统 `PATH` 查找，且不创建 packaged binary。
- [x] Phase 5 optional package manifest 预检：`e5b92fa7 feat(rust-go): 补齐 Phase 5 optional package manifest 预检` 已新增 `native-runtime-package-manifest.ts`，固定 darwin arm64 / darwin x64 / win32 x64 / linux x64 四个平台包计划，校验 package version、protocol / cache schema、platform / arch、binary name 和 SHA-256 fingerprint，并用 exact-key 白名单拒绝 `binaryPath`、home 和 path-like 额外字段。`smoke:native-runtime -- --mode packaged-manifest` 只验证 manifest schema / 平台矩阵和 TS fallback 可用，summary 明确 `bundledBinaryVerified=false`；它不是真实 bundled binary packaged smoke。
- [x] Phase 5 bundled package resolver fixture：`ce104a59 feat(rust-go): 补齐 Phase 5 bundled package resolver fixture` 已新增 `native-runtime-package-resolver.ts`，只从 optional package `package.json` 派生包根，读取包内固定 `native-search-package.json` 和 `bin/{binaryName}`，校验 app `node_modules` realpath allowlist、manifest schema、平台矩阵、可执行权限和 SHA-256 fingerprint；service 只在 Electron packaged 环境自动尝试 resolver，非 packaged dev 仍要求显式 `CODEINSIGHTS_NATIVE_SEARCH_BINARY`。`packaged-manifest` smoke 已包含临时 optional package resolver fixture，summary 明确 `fixtureBundledPackageVerified=true`、`usesTemporaryFixture=true`、`realPackagedBinaryVerified=false`；它仍不是真实 packaged app bundled binary smoke。
- [x] Phase 5 packaged app layout preflight smoke：`52613780 feat(rust-go): 补齐 Phase 5 packaged app layout 预检 smoke` 已新增只读入口，`edeba827 feat(rust-go): 补齐 Phase 5 packaged smoke invocation plan` 已补充 `--packaged-app-root` 调用入口。默认无 `--packaged-app-root` 或 `--app-node-modules-root` 时 skipped 且 TS fallback 可用；推荐显式传入 packaged app root，legacy `--app-node-modules-root` 仍兼容且优先，但缺失路径必须进入 `app_node_modules_root_unresolved`。resolver 校验 optional package manifest、`bin/{binaryName}`、可执行权限、SHA-256 和 app `node_modules` allowlist，不走系统 `PATH`。summary 拆分 `packagedAppLayoutVerified`、`packagedAppEvidenceVerified`、`usesTemporaryFixture`、`realPackagedBinaryVerified`；临时 fixture 只能证明 layout，不能把 `bundledBinaryVerified` 或 `realPackagedBinaryVerified` 置为 true。它仍不是真实 packaged app bundled binary smoke。
- [x] Phase 5 packaged app evidence / identity gate：`800dc885 feat(rust-go): 补齐 Phase 5 packaged app evidence smoke` 已把 `packaged-app-layout` evidence classifier 扩展为同时支持 `app.asar` + `app.asar.unpacked/node_modules` 和当前 `electron-builder.yml` 的 `asar: false` 布局（`Resources/app/node_modules` + `Resources/app/package.json`）；`31e37cbb fix(rust-go): 收紧 Phase 5 packaged app evidence 真实验证` 已新增 `packagedAppIdentityVerified` gate，当前只让 `unpacked-app` 通过 app `package.json` 的 `name="@codeinsights/electron"` 与 `main="dist/main.cjs"` identity 校验。detail 只输出 `packagedAppEvidence=asar-unpacked` / `unpacked-app` / `none`、package name 和 resolver reason code，不输出 packaged root 或 binary path；临时 fixture、缺少 identity 或缺少真实 optional package / binary 时都不能证明真实 bundled binary。
- [x] Phase 5 optionalDependencies declaration preflight gate：`563b804c feat(rust-go): 补齐 Phase 5 optionalDependencies 声明预检 gate` 已新增 `validateNativeSearchOptionalDependencies()`，只校验传入 package manifest 的 `optionalDependencies` 是否覆盖 4 个 native search 平台包；后续 `1114233b` 已把版本声明收紧为 exact semver 或 `npm:<同名 native search package>@exact-semver`，拒绝空值、非字符串、`latest` / range、跨包 alias、`file:`、`workspace:`、git / http URL 和 path-like spec。不读取真实 `node_modules`、不解析 binary、不证明 package 已安装。`packaged-manifest` smoke 新增 `packaged-optional-dependencies-preflight` case；当前 `apps/electron/package.json` 尚未声明 `@codeinsights/native-search-*`，summary 明确 `optionalDependenciesDeclared=false`、4 个 `missingOptionalDependencies`、`invalidOptionalDependencies=[]`、`realPackagedBinaryVerified=false`。`packaged-app-layout` 的真实验证 gate 也已要求 `optionalDependenciesDeclared=true`，且 `native-runtime-smoke` CLI 遇到任意 failed case 会返回非零退出码。
- [x] Phase 5 optional package install-chain preflight gate：`1114233b feat(rust-go): 补齐 Phase 5 optional package 安装链路预检` 已新增 `validateNativeSearchOptionalPackageInstallChain()`，只读校验 native search optionalDependencies 声明、`bun.lock` resolved package entry 和 installed package manifest 一致性；`63e0fc2b feat(rust-go): 外显 no-go audit 并加固 install-chain manifest` 已进一步要求 installed package manifest 满足 optional package source 的严格 package.json shape，包含 exact version、`os` / `cpu` / `bin` / `files` / `publishConfig.access=public`，并拒绝 lifecycle scripts 与 runtime dependency 字段。当前仓库未声明真实 `@codeinsights/native-search-*` optionalDependencies，因此 summary 保持 `optionalDependenciesInstallChainVerified=false`、`optionalDependenciesLockfileVerified=false`、`optionalDependenciesInstalledPackagesVerified=false`、`realPackagedBinaryVerified=false`。声明存在但 lockfile / installed package manifest 不完整或 metadata 不合规时 install-chain smoke 会 failed，不会 skipped。
- [x] Phase 5 default-enable readiness 预检：`2bf88a5a feat(rust-go): 补齐 Phase 5 default-enable readiness 预检` 已新增 `nativeSearchDefaultEnableReadiness`，把 benchmark gate、Agent facade extractor / parity、optionalDependencies declaration、optional package install-chain、packaged app evidence / identity、real packaged binary 和人工风险复核收敛为机器可读 gate。`native-runtime:benchmark` 与 `smoke:native-runtime` summary 均输出 `defaultEnableCandidate=false`、`explicitOptInRequired=true`；顶层 `realPackagedBinaryVerified` / `bundledBinaryVerified` 已收紧为同时要求 install-chain、packaged evidence、packaged identity 和真实 binary verification。
- [x] Phase 5 packaging config allowlist 预检：`348e003d feat(rust-go): 补齐 Phase 5 packaging config allowlist 预检` 已新增 `validateNativeSearchPackagingConfig()`，只读解析 `electron-builder.yml` 顶层 `files:` 列表，要求 4 个计划 native search optional packages 都有显式 per-package include。当前仓库输出 `packagingConfigVerified=false`、4 个 `missingPackagingConfigPackages` 和 `blockingPackagingConfigExcludes=["!node_modules/@codeinsights/**"]`；`nativeSearchDefaultEnableReadiness` 新增 `packaging_config_not_verified` blocker，`realPackagedBinaryVerified` / `bundledBinaryVerified` 必须同时受 optional install-chain、packaging config、packaged evidence、packaged identity 和真实 binary verification 约束。
- [x] Phase 5 optional package 发布状态预检：`6ae1c896 feat(rust-go): 补齐 Phase 5 optional package 发布状态预检` 已新增 `validateNativeSearchOptionalPackagePublication()`、`getNativeSearchOptionalDependencyExpectedVersions()` 和 `--check-registry` 显式只读 registry gate。默认 `packaged-manifest` / `packaged-app-layout` 不联网，publication case skipped 且 `optionalPackagesPublished=false`；显式 `--check-registry` 会只读查询 npm packument，当前 4 个计划 package 全部进入 `missingPublishedOptionalPackages`，smoke exit 1，`optional_packages_not_published` blocker 生效。publication helper 绑定 optionalDependency exact version 到 `versions[expectedVersion]`，不能只看 latest；temporary fixture、publication-only、packaging-only 或 install-chain-only 都不能让 `realPackagedBinaryVerified=true`。
- [x] Phase 5 packaged bundled binary smoke 执行计划预检：`dedbfed1 feat(rust-go): 补齐 Phase 5 packaged smoke 执行计划预检` 已在 `smoke:native-runtime` summary 中新增 `packagedBundledBinarySmokePlan`，输出 `blocked` / `ready` / `verified` 三态、阻塞项、required inputs、allowed / forbidden actions 和候选命令。当前默认 `packaged-manifest` / `packaged-app-layout` 都为 `status="blocked"`；`ready` 不是 verified，`verified` 必须同时满足顶层 `bundledBinaryVerified=true` 且无 blocker。
- [x] Phase 5 optional package publish-target dry-run 预检：`9e3e3dff feat(rust-go): 补齐 Phase 5 optional package publish target 预检` 已新增 `validateNativeSearchOptionalPackagePublishTarget()` 和 `smoke:native-runtime -- --mode optional-package-publish-target --native-search-package-version 0.0.3`。默认不联网时保持 `optionalPackagePublishTargetChecked=false`、`optionalPackagePublishTargetReady=false`；显式 `--check-registry` 只读检查目标版本是否已碰撞。`dc75f380` 已将 `native/search/src/lib.rs` 的 `BINARY_VERSION` 与 Cargo version 对齐为 `0.0.3`，因此当前 4 个 planned packages 的 `0.0.3` 目标版本可发布、无 registry collision、source version consistency 通过，显式 registry dry-run 输出 `optionalPackagePublishTargetReady=true`。该 dry-run 不等于 package 已发布，不改变 `optionalPackagesPublished`、`realPackagedBinaryVerified`、`bundledBinaryVerified` 或 default enable。
- [x] Phase 5 release-ready source version 前置：`dc75f380 feat(rust-go): 对齐 Phase 5 native search release source version` 已将 Rust sidecar source `BINARY_VERSION` 从 `0.0.3-dev` 调整为 `0.0.3`，使 publish-target dry-run 可证明目标版本 release-ready；未发布 npm package，未创建 release / packaged native binary，未声明 / 安装真实 optionalDependencies，未修改 builder allowlist。
- [x] Phase 5 optional package source preflight：`05c393bb feat(rust-go): 补齐 Phase 5 optional package source 预检` 已新增 `validateNativeSearchOptionalPackageSources()` 和 `smoke:native-runtime -- --mode optional-package-source --native-search-package-version 0.0.3`。该 preflight 只验证 planned in-memory npm package source blueprint：exact name / version、`private=false`、description / license、`os` / `cpu`、`bin.codeinsights-native-search`、精确 `files` allowlist、`publishConfig.access=public` 和闭合 `native-search-package.json` 计划；拒绝 path-like 字段、运行时 dependency 字段和 install / publish lifecycle scripts。当前 `optionalPackageSourceReady=true`，但它不运行 `npm pack`、不发布、不安装、不读取 binary、不证明 SHA、不修改 optionalDependencies / builder / packaged smoke。
- [x] Phase 5 optional / packaged gate 执行顺序预检：`ce89f490 feat(rust-go): 补齐 Phase 5 optional packaged 执行顺序预检` 已新增 `buildNativeSearchOptionalPackageExecutionPlan()` 和 `optionalPackageExecutionPlan` smoke summary。该 plan 输出 `nextStage`、严格顺序前缀 `completedPrerequisites`、独立证据 `observedEvidenceStages`、`blockedBy`、ready flags、allowed / forbidden actions 和 candidate commands；source / publish-target ready 必须先受 checked gate 约束，二者同时 ready 时也只进入 `optional_package_publication`，不证明 package 已发布、optionalDependencies 已声明 / 安装、builder allowlist 已修改、真实 packaged binary 已验证或 default enable 可行。
- [x] Phase 5 builder allowlist dry-run plan：`a3f98673 feat(rust-go): 补齐 Phase 5 builder allowlist dry-run plan` 已新增 `buildNativeSearchPackagingConfigAllowlistChangePlan()` 和 `packagingConfigAllowlistChangePlan` smoke summary。该 plan 只读输出 builder allowlist 实际修改前的 review 输入：4 个精确 required / missing include、当前 blocker `!node_modules/@codeinsights/**`、宽泛 include 禁止项、removal candidates、候选验证命令和 forbidden actions；当前为 `status="blocked"`、`approvalRequired=true`。它不修改 `apps/electron/electron-builder.yml`，不声明 / 安装 optionalDependencies，不发布 package，不读取 binary，也不证明 packaged app bundled binary。
- [x] Phase 5 optionalDependencies install-chain dry-run plan：`7726a008 feat(rust-go): 补齐 Phase 5 optionalDependencies install-chain dry-run plan` 已新增 `buildNativeSearchOptionalDependenciesInstallChainChangePlan()` 和 `optionalDependenciesInstallChainChangePlan` smoke summary。该 plan 只读输出真实 optionalDependencies 声明与安装执行前的 review 输入：4 个 planned optionalDependency exact spec（当前 `0.0.3`）、publication / declaration / install-chain / lockfile / installed package blockers、候选验证命令和 forbidden actions；当前为 `status="blocked"`、`approvalRequired=true`。它不写 `apps/electron/package.json` 或 `bun.lock`，不安装、不发布、不修改 builder、不读取 binary，也不证明 packaged app bundled binary。
- [x] Phase 5 optional package publication change plan 证据拆分：`8097ed4c feat(rust-go): 补齐 Phase 5 optional package publication change plan` 新增 `optionalPackagePublicationChangePlan`，`2188e276 fix(rust-go): 收紧 Phase 5 publication plan collision 过滤` 和 `d679eabb fix(rust-go): 拆分 Phase 5 publication plan 发布证据` 已收紧语义：`publishedPackages` 只表示 registry 中 exact expected version 且 metadata 有效的真实 publication 证据；`publishTargetCollisionPackages` 与 `existingInvalidPublishedPackages` 只进入 `commandExcludedPackages` 并排除候选发布命令，不能当作已发布证据。该 plan 只是真实 publication 前 review 输入，不运行 `npm publish` / `npm pack`，不安装、不写 package manifest / lockfile / builder，不读取 binary，也不证明 packaged app bundled binary。
- [x] Phase 5 packaged smoke invocation plan：`edeba827 feat(rust-go): 补齐 Phase 5 packaged smoke invocation plan` 已新增 `packagedBundledBinarySmokeInvocationPlan`，把真实 packaged smoke 的调用输入、候选命令、legacy 兼容命令、验收证据、blocked reason 和 forbidden actions 机器可读化。候选命令优先使用 `--packaged-app-root <packaged-app-root> --check-registry`，legacy 命令使用 `--app-node-modules-root <packaged-app-node_modules> --check-registry`；`ready_for_execution` 不等于 verified，`verified` 仍必须绑定 `bundledBinaryVerified=true` 且无 blocker。`3f5a0783` 后该 plan 还包含 `executionDesign`，记录 preferred / legacy input mode、registry check requirement、real packaged app requirement 和禁止临时 fixture 当作真实 evidence；该 plan 不创建 packaged app、不发布、不安装、不改 builder、不读取 binary，也不输出 packaged app root、node_modules root 或 binary path。
- [x] Phase 5 optional package publication invocation plan：`f949fc13 feat(rust-go): 补齐 Phase 5 optional package publication invocation plan` 已新增 `optionalPackagePublicationInvocationPlan`，把真实 `npm publish` 前的 required inputs、candidate commands、per-package invocation blockers、acceptance evidence 和 forbidden actions 机器可读化；`ready_for_invocation` 只表示可进入人工审核 / 调用，不发布 package、不运行 `npm pack`、不安装、不写 package manifest / lockfile / builder、不读取 binary。
- [x] Phase 5 publication invocation 发布证据收紧：`77b266e8 fix(rust-go): 收紧 Phase 5 publication invocation 发布证据` 已收紧 invocation plan 的 `publishedPackages` 来源：只从 package-level `status="published"` 汇总，且 fully verified 必须要求 planned / published package 集合唯一并严格匹配；invalid、unavailable、duplicate、collision 或矛盾 evidence 只能进入 blocker / exclusion，不能当作已发布。
- [x] Phase 5 release handoff 证据计划：`4fec7fab feat(rust-go): 补齐 Phase 5 release handoff 证据计划` 已新增 `nativeSearchReleaseHandoffPlan`，聚合 publication invocation、optionalDependencies install-chain change plan、builder allowlist change plan、packaged smoke invocation plan 和 execution/default-enable gate；`ready_for_release_handoff` 只表示当前顺序 gate 可交接人工审核 / 执行，不证明真实 publish / install / builder 修改 / packaged smoke / default enable。
- [x] Phase 5 release handoff fail-closed 证据收紧：`3f5a0783 feat(rust-go): 收紧 Phase 5 release handoff fail-closed 证据` 已新增 `nativeSearchReleaseHandoffPlan.gateBinding`，把 authoritative next gate 绑定到 `optionalPackageExecutionPlan.nextStage`，并要求 verified 绑定 execution plan verified；后置 publication / packaged smoke evidence 不能跳过上游 gate，乱序 evidence 必须 fail closed。该提交同时补齐 packaged smoke invocation 的 `executionDesign`，但不验证 publication / install / builder / packaged / default-enable。
- [x] Phase 5 builder allowlist 实际修改前置证据：`ddc818ae feat(rust-go): 补齐 Phase 5 builder allowlist 修改意图` 已新增 `packagingConfigAllowlistChangePlan.candidateYamlEditPlan`，只读记录后续真实 `apps/electron/electron-builder.yml` 修改的目标文件、`modifiesFile=false`、操作顺序、待添加精确 per-package include、需要移除的阻断规则、禁止宽泛 include 和验收证据；它不修改 builder、不发布、不安装、不读取 binary，也不证明 packaged app bundled binary。
- [x] Phase 5 builder allowlist 修改意图状态同步：`f02ceae0 docs(rust-go): 同步 Phase 5 builder allowlist 修改意图状态` 与 `3089b768 docs(rust-go): 回填 Phase 5 最新状态和启动提示词` 已回填最新完成 / 未完成状态、恢复入口和下次启动提示词；它们只更新文档，不改变 native default off / 显式 opt-in 边界。
- [x] Phase 5 release handoff 批准包：`99d76b0c feat(rust-go): 补齐 Phase 5 release handoff 批准包` 已新增 `nativeSearchReleaseHandoffPlan.currentGateApprovalPacket`，按当前 `optionalPackageExecutionPlan.nextStage` 输出批准状态、所需 approval、前置证据、候选命令、授权边界和禁止动作。blocked gate 不输出可执行候选命令；publication gate ready 时只授权 release approval 后的候选 publish 调用审核，不授权 package.json / bun.lock / builder / packaged app / default enable。
- [x] Phase 5 release handoff 批准包状态同步：`bd0e61d3 docs(rust-go): 同步 Phase 5 release handoff 批准包状态` 已回填 development checklist、sidecar protocol / smoke plan、`next-session-prompt.md`、`tasks/todo.md` Review 和 lessons；它只更新文档，不改变真实 optional package 发布 / install-chain / builder / packaged smoke gate。
- [x] Phase 5 release handoff 全 gate 批准队列：`197569d5 feat(rust-go): 补齐 Phase 5 release handoff 批准队列` 已新增 `nativeSearchReleaseHandoffPlan.gateApprovalQueue`，按 publication、optionalDependencies declaration、install-chain、builder allowlist、packaged smoke 和 default-enable risk review 的真实顺序输出批准矩阵。只有当前且 ready 的 gate 暴露候选命令；后续 gate 必须保持 `blocked_until_prior_gate_verified` 且 `candidateCommands=[]`，已完成 gate 只能标为 `verified`，不能被当成重复执行授权。
- [x] Phase 5 release handoff 批准队列状态同步：`9d8520fa docs(rust-go): 同步 Phase 5 release handoff 批准队列状态` 已回填 development checklist、sidecar protocol / smoke plan、`next-session-prompt.md`、`tasks/todo.md` Review 和 lessons；它只更新文档，不改变真实 optional package 发布 / install-chain / builder / packaged smoke gate。
- [x] Phase 5 release handoff gate 执行证据清单：`cb30b819 feat(rust-go): 补齐 Phase 5 release handoff 执行证据清单` 已新增 `nativeSearchReleaseHandoffPlan.gateExecutionEvidenceChecklist`，从全 gate approval queue 只读派生每个 gate 批准执行后必须补齐的 evidence、当前 gate 批准后验证命令、文件 / 网络 / 远端写副作用标记和 fail-closed 边界；blocked future gate 继续不暴露执行命令，不证明真实 publication / install-chain / builder / packaged binary / default enable。
- [x] Phase 5 release handoff default-enable 网络副作用边界：`72a00b01 fix(rust-go): 标记 default enable 证据清单网络边界` 已把 `default_enable_risk_review` 的 `networkRequired` 标记修正为 true，并补充 full gate `networkRequired` 矩阵断言；blocked future gate 仍保持 `postExecutionVerificationCommands=[]`，不授权联网执行。
- [x] Phase 5 release handoff gate 执行后状态迁移期望：`b874628b feat(rust-go): 补齐 Phase 5 gate 状态迁移期望` 已为 `gateExecutionEvidenceChecklist` 新增 `expectedNextGateAfterVerification`、`expectedSummaryFlagsAfterExecution`、`mustRemainUnverifiedAfterExecution` 和 `transitionFailClosedCriteria`；这些字段只说明真实 gate 执行并验证后应如何验收 summary，不证明真实 publication / install-chain / builder / packaged binary / default enable。
- [x] Phase 5 gate 状态迁移期望状态同步：`da6d79dc docs(rust-go): 同步 Phase 5 gate 状态迁移期望` 已回填 development checklist、sidecar protocol / smoke plan、`next-session-prompt.md` 和 `tasks/todo.md` Review，明确 transition expectation 仍只是执行后验收输入，真实 optional package 发布 / install-chain / builder / packaged smoke / default-enable 仍未完成。
- [x] Phase 5 release handoff gate 迁移校验器：`66da7eba feat(rust-go): 补齐 Phase 5 gate 迁移校验器` 已新增 `evaluateNativeSearchReleaseHandoffGateTransition()`，用 before / after summary fail-closed 校验当前 gate readiness、expected next gate、expected summary flags、must-remain-unverified flags 和 no-go boundary violations；该校验器不发布、不安装、不写 builder、不创建 packaged app、不读取 binary，也不证明 packaged verified 或 default-enable。
- [x] Phase 5 release handoff 执行输入证据包：`461295b0 feat(rust-go): 补齐 Phase 5 release handoff 执行输入证据包` 已新增 `nativeSearchReleaseHandoffPlan.currentGateExecutionInputPacket`，只读输出当前顺序 gate 的批准前输入、批准后允许动作、候选命令、执行后验证命令、副作用边界和验收证据；该 packet 不发布、不安装、不写 builder、不创建 packaged app、不读取 binary，也不证明 packaged verified 或 default-enable。
- [x] Phase 5 release handoff no-go 边界审计：`8a0bcfd5 feat(rust-go): 补齐 Phase 5 release handoff no-go 边界审计` 已新增 `evaluateNativeSearchReleaseHandoffNoGoBoundary()`，只读校验 default off / explicit opt-in、blocked / future gate candidate command、post-execution command、副作用、premature verified flag 和敏感字符串边界；该 audit 不执行命令、不联网、不写文件、不读取 binary path，也不证明真实 publication、install-chain、builder verified、packaged binary verified 或 default-enable。
- [x] Phase 5 no-go audit summary 与 install-chain metadata 加固：`63e0fc2b feat(rust-go): 外显 no-go audit 并加固 install-chain manifest` 已将 `nativeSearchReleaseHandoffNoGoBoundaryAudit` 外显到 smoke summary，并追加 `release-handoff-no-go-boundary-audit` case；audit 失败会让 smoke exit code fail closed。该提交同时加固 installed package manifest metadata 校验，但仍不发布、不安装、不声明真实 optionalDependencies、不修改 builder、不创建 packaged app / binary、不证明 default enable。
- [x] Phase 5 no-go audit summary 状态同步：`885e89b5 docs(rust-go): 同步 Phase 5 no-go audit summary 状态` 已回填 development checklist、sidecar protocol / smoke plan、`next-session-prompt.md`、`tasks/todo.md` Review 和 `tasks/lessons.md`；它只更新文档，不改变真实 optional / packaged gate。
- [x] Phase 5 后续计划缩减状态同步：本轮将后续开发压缩为最小真实 optional package gate、最小 install-chain gate 和最小 packaged smoke gate；default-enable、Go supervisor、Phase 6 / 7 / 9、全平台 CI / 发布矩阵和 helper-only 新增工作均暂缓。
- [x] Phase 5 optional package / Agent work-delay 只读复核：当前 4 个计划 `@codeinsights/native-search-*` 包在 npm registry 查询为 `E404`，且 packaging config allowlist 预检明确当前 builder files 缺少 per-package include 并被 `!node_modules/@codeinsights/**` 阻断，因此不能在不发布真实包 / 不获准调整 builder allowlist 的前提下安全声明并安装真实 optionalDependencies。Agent native work-delay 小幅回退更可能来自 benchmark 口径、sidecar hop 固定成本和短调用噪声；Agent nested native parity 已完成，但仍不能替代 packaged / optional / builder allowlist gate。
- [x] Phase 5 后续范围收窄状态同步：`4518a2ce docs(rust-go): 收窄 Phase 5 后续范围` 已把当前任务计划收敛为 Phase 5 verified / release handoff 收口；Phase 6、Phase 7、Phase 9 暂停；Phase 8 不作为全量打包 / CI / 发布阶段推进，仅保留 Phase 5 packaged smoke 所需的最小 packaged 验证设计。

未完成：

- [~] Phase 5：Rust search sidecar 试点已完成到 no-go audit summary / smoke case 外显和 install-chain installed manifest metadata 加固；后续只保留 3 个核心 gate：最小真实 optional package gate、最小 install-chain gate、最小 packaged smoke gate。真实 optional package 发布、optionalDependencies 实际声明与安装执行、builder allowlist 实际修改、真实 packaged app bundled binary smoke 尚未完成。
- [skip] Phase 6：大文件 / 日志 chunk preview 暂停，不进入当前目标；风险接受人：用户，2026-06-07。
- [skip] Phase 7：PathSafety 与 GitOutputParser 抽象暂停，不进入当前目标；风险接受人：用户，2026-06-07。
- [min] Phase 8：全量打包、CI、版本与发布收口暂停；当前只保留 Phase 5 一个真实 packaged app bundled binary smoke 所需的最小 packaged 验证设计，不做完整 release scope。
- [skip] Phase 9：Go supervisor 可选 spike 暂停，不进入当前目标；风险接受人：用户，2026-06-07。

关键边界：

- [ ] 当前没有 packaged native binary；Electron main process 已具备显式 opt-in native search 调用链，但默认关闭。
- [ ] 当前后续计划已压缩为 3 个核心 gate：最小真实 optional package gate、最小 install-chain gate、最小 packaged smoke gate。Phase 6、Phase 7、Phase 9 不做，Phase 8 不作为独立全量阶段推进。
- [ ] Phase 5 下一步优先推进最小真实 optional package gate：只先处理当前可验证平台的 package source、发布和 registry evidence；多平台完整矩阵暂缓。
- [ ] 完成最小真实 optional package gate 后，再推进最小 install-chain gate：声明对应 optionalDependency、执行真实 install，验证 `bun.lock`、installed package manifest metadata 和 native default off 不变。
- [ ] 完成 install-chain 后，再推进最小 packaged smoke gate：只做精确 builder allowlist 修改和一个真实 packaged app bundled binary smoke，证明 app 内 `node_modules` 的 native binary 可解析、可执行且不走系统 `PATH`。
- [defer] default-enable 继续暂缓；即使最小 packaged opt-in 通过，也只记录“packaged opt-in 可用”，不默认启用 native。
- [skip] 不再新增 helper-only approval / handoff / dry-run 代码，除非真实 gate 执行暴露明确缺口；`publishedPackages` 仍只表示真实 registry publication evidence，collision / invalid / unavailable / duplicate metadata 不能当作已发布。
- [ ] Phase 5 实现必须遵守已写好的依赖 decision record、sidecar protocol、contract parity、native missing / crash / timeout fallback、packaged smoke 和安全门禁。
- [ ] 不修改根 `README.md` / 根 `AGENTS.md`，除非用户明确授权。
- [ ] 不 push、不创建 PR、不执行真实远端写，除非用户明确要求。

## 可复制提示词

```text
请继续 CodeInsights Rust / Go 优化重构迭代。先读取 tasks/lessons.md、tasks/todo.md、docs/improve/rust-go/2026-06-01-rust-go-optimization-plan.md、docs/improve/rust-go/2026-06-01-rust-go-development-checklist.md、docs/improve/rust-go/2026-06-03-phase-5-dependency-decision-record.md、docs/improve/rust-go/2026-06-03-phase-5-sidecar-protocol-and-smoke-plan.md、docs/improve/rust-go/next-session-prompt.md 和 native/search/。

当前进度：Rust / Go 优化方案、开发跟踪清单、阶段提交纪律、Phase 0“基线、契约与 benchmark”、Phase 1“TypeScript fallback 与 EventSearchService 重构”、Phase 2“Pipeline records cursor / tail 与全局搜索接入”、Phase 3“Workspace 文件索引 TS cache 与 watcher invalidation”和 Phase 4“前端可见体验、Jotai 状态和 diagnostics”已经完成。

Phase 5 已完成前置计划、依赖 decision record、sidecar protocol / fallback / packaged smoke 计划、100MB 级 TS fallback benchmark gate、`native/search/` 最小 Rust search-only sidecar 源码切片、Electron main process sidecar manager、Chat search fallback gate、Rust vs TS contract parity、100MB native benchmark 对比、Rust search 早停性能优化、2 轮稳定 benchmark 复跑、benchmark event-loop 口径增强、新字段稳定 benchmark 复跑、native benchmark gate 汇总、Agent production facade benchmark 分析、Agent nested native parity、基础 `smoke:native-runtime` 脚本、native-cache manifest schema helper、`protocol-mismatch` / `crash` / `timeout` / `cache-corruption` fake sidecar / isolated cache smoke、optional package manifest schema helper / `packaged-manifest` 预检、bundled package resolver fixture、packaged app layout preflight smoke、packaged app evidence classifier、`packagedAppIdentityVerified` gate、`563b804c feat(rust-go): 补齐 Phase 5 optionalDependencies 声明预检 gate`、`1114233b feat(rust-go): 补齐 Phase 5 optional package 安装链路预检`、`f86553ee feat(rust-go): 补齐 Phase 5 native benchmark gate 汇总`、`9a06908a feat(rust-go): 补齐 Phase 5 Agent facade benchmark 分析`、`665c5db7 feat(rust-go): 补齐 Phase 5 Agent nested native parity`、`2bf88a5a feat(rust-go): 补齐 Phase 5 default-enable readiness 预检`、`348e003d feat(rust-go): 补齐 Phase 5 packaging config allowlist 预检`、`6ae1c896 feat(rust-go): 补齐 Phase 5 optional package 发布状态预检`、`dedbfed1 feat(rust-go): 补齐 Phase 5 packaged smoke 执行计划预检`、`9e3e3dff feat(rust-go): 补齐 Phase 5 optional package publish target 预检`、`dc75f380 feat(rust-go): 对齐 Phase 5 native search release source version`、`05c393bb feat(rust-go): 补齐 Phase 5 optional package source 预检`、`ce89f490 feat(rust-go): 补齐 Phase 5 optional packaged 执行顺序预检`、`a3f98673 feat(rust-go): 补齐 Phase 5 builder allowlist dry-run plan`、`7726a008 feat(rust-go): 补齐 Phase 5 optionalDependencies install-chain dry-run plan`、`8097ed4c feat(rust-go): 补齐 Phase 5 optional package publication change plan`、`2188e276 fix(rust-go): 收紧 Phase 5 publication plan collision 过滤`、`d679eabb fix(rust-go): 拆分 Phase 5 publication plan 发布证据`、`edeba827 feat(rust-go): 补齐 Phase 5 packaged smoke invocation plan`、`f949fc13 feat(rust-go): 补齐 Phase 5 optional package publication invocation plan`、`77b266e8 fix(rust-go): 收紧 Phase 5 publication invocation 发布证据`、`4fec7fab feat(rust-go): 补齐 Phase 5 release handoff 证据计划`、`3f5a0783 feat(rust-go): 收紧 Phase 5 release handoff fail-closed 证据`、`ddc818ae feat(rust-go): 补齐 Phase 5 builder allowlist 修改意图`、`99d76b0c feat(rust-go): 补齐 Phase 5 release handoff 批准包`、`197569d5 feat(rust-go): 补齐 Phase 5 release handoff 批准队列`、`bc53cc75 docs(rust-go): 回填 Phase 5 批准队列最新恢复入口`、`cb30b819 feat(rust-go): 补齐 Phase 5 release handoff 执行证据清单`、`72a00b01 fix(rust-go): 标记 default enable 证据清单网络边界`、`b874628b feat(rust-go): 补齐 Phase 5 gate 状态迁移期望`、`da6d79dc docs(rust-go): 同步 Phase 5 gate 状态迁移期望`、`aa0091a3 docs(rust-go): 回填 Phase 5 最新恢复入口`、`40366181 docs(rust-go): 同步 Phase 5 gate 迁移校验器状态`、`66da7eba feat(rust-go): 补齐 Phase 5 gate 迁移校验器`、`5be9c233 docs(rust-go): 回填 Phase 5 最新开发状态`、`461295b0 feat(rust-go): 补齐 Phase 5 release handoff 执行输入证据包`、`7898dae6 docs(rust-go): 同步 Phase 5 执行输入证据包状态`，`8a0bcfd5 feat(rust-go): 补齐 Phase 5 release handoff no-go 边界审计`，以及 `63e0fc2b feat(rust-go): 外显 no-go audit 并加固 install-chain manifest`。最新已确认实现基线是 `63e0fc2b feat(rust-go): 外显 no-go audit 并加固 install-chain manifest`，最新已确认状态同步 / 恢复入口是 `345dcf01 docs(rust-go): 同步 Phase 5 no-go 边界审计状态`；若本轮再次产生状态同步提交，下次启动以 `git log -5 --oneline` 中最新的 Rust / Go docs 提交为实际恢复入口。

已完成的 Phase 5 sidecar manager 切片：Rust crate 直接依赖固定为 `serde = 1.0.228`、`serde_json = 1.0.150`；实现 stdin / stdout line-delimited JSON protocol、`status`、literal `search`、`shutdown` 和 explicit out-of-scope `tail_jsonl` typed error；Electron main process 通过 `native-runtime-sidecar-manager.ts` 显式 binary path opt-in，不从系统 PATH 查找；覆盖 missing binary、version mismatch、timeout、crash、shutdown、bad JSON stdout、stdout buffer 上限、contract violation、source boundary violation、range violation、拒绝类错误不 fallback、Chat legacy snippet parity、真实 Rust vs TS parity。`0eb350ff` 已把 Rust search 升级到 `0.0.2` / `0.0.2-dev` 并完成 `limit + 1` 早停；`665c5db7` 已把 Rust search 升级到 `0.0.3` / `0.0.3-dev` 并新增受限白名单 extractor `agent_message_search_text`；`dc75f380` 已将 source `BINARY_VERSION` 对齐为 `0.0.3`。

基础 smoke/cache 切片：`smoke:native-runtime` 支持 native-missing / native-available / protocol-mismatch / crash / timeout / cache-corruption / packaged-manifest / packaged-app-layout / optional-package-publish-target / optional-package-source 入口；native available 无显式 binary 时 skipped，不从 PATH 查找；native-cache manifest root 固定为 `getConfigDir()/native-cache`，记录 schema / protocol / package plan，不记录 binary path，损坏 manifest 返回 `cache_corrupted`。`e5b92fa7` 已补齐 optional package manifest schema helper；`ce104a59` 已补齐 bundled package resolver fixture；`800dc885` / `31e37cbb` 已补齐 packaged app evidence / identity gate。`563b804c` 新增 optionalDependencies declaration preflight，`1114233b` 新增 optional package install-chain preflight。`2bf88a5a` 已新增 `nativeSearchDefaultEnableReadiness`；`348e003d` 新增 packaging config allowlist 预检，当前 smoke 输出 `packagingConfigVerified=false`、4 个 `missingPackagingConfigPackages` 和 `blockingPackagingConfigExcludes=[\"!node_modules/@codeinsights/**\"]`。`6ae1c896` 新增 optional package 发布状态预检：默认 smoke 不联网，`optionalPackagePublicationChecked=false`、`optionalPackagesPublished=false`；显式 publication `--check-registry` 只读查询 npm packument，当前 4 个计划包全部进入 `missingPublishedOptionalPackages`，`packaged-manifest --check-registry` 和 `packaged-app-layout --check-registry` 都会 exit 1，`optional_packages_not_published` blocker 生效。`dedbfed1` 新增 `packagedBundledBinarySmokePlan`：默认 `packaged-manifest` / `packaged-app-layout` 都输出 `status=\"blocked\"`，blockers 包含 `optional_packages_not_published`、`optional_dependencies_not_declared`、`optional_package_install_chain_not_verified`、`packaging_config_not_verified` 和 `prebuilt_packaged_app_required`；`ready` 只表示前置输入具备，`verified` 必须同时满足顶层 `bundledBinaryVerified=true` 且无 blocker。`05c393bb` 新增 `optional-package-source`：当前 `optionalPackageSourceReady=true`、4 个 planned package source blueprint 形状通过，但不运行 `npm pack`、不发布、不安装、不读取 binary、不证明 SHA。`ce89f490` 新增 `optionalPackageExecutionPlan`：当前 source / publish-target 同时 ready 时只进入 `optional_package_publication`，不会把 ready 误判为 published / installed / packaged verified。`a3f98673` 新增 `packagingConfigAllowlistChangePlan`：当前 `status=\"blocked\"`、`approvalRequired=true`，输出 4 个 required / missing includes、`blockingExcludes=[\"!node_modules/@codeinsights/**\"]`、too-broad include 禁止项和候选验证命令；它只做 dry-run review，不修改 builder、不证明 packaging config verified。`7726a008` 新增 `optionalDependenciesInstallChainChangePlan`：当前 `status=\"blocked\"`、`approvalRequired=true`，输出 4 个 `0.0.3` exact optionalDependency spec、publication / declaration / install-chain / lockfile / installed package blockers 和候选验证命令；它只做声明与安装执行前 review 输入，不写 `package.json` / `bun.lock`，不安装、不发布、不读取 binary、不证明 packaged verified。`8097ed4c` / `2188e276` / `d679eabb` 新增并收紧 `optionalPackagePublicationChangePlan`：显式 registry publish-target dry-run 与 source preflight 都 ready 时可进入 `ready_for_review`，但 `publishedPackages` 仍为空；target collision 或 exact-version invalid registry metadata 只进入 `publishTargetCollisionPackages` / `existingInvalidPublishedPackages` / `commandExcludedPackages`，不能当作已发布证据。`edeba827` 新增 `packagedBundledBinarySmokeInvocationPlan`：推荐命令使用 `--packaged-app-root <packaged-app-root> --check-registry`，legacy 命令使用 `--app-node-modules-root <packaged-app-node_modules> --check-registry`；`ready_for_execution` 只表示调用输入和前置 gate 齐备，不等于真实 packaged binary verified。`f949fc13` 新增 `optionalPackagePublicationInvocationPlan`：当前 package 未发布时可进入 `ready_for_invocation`，但它只输出 publish 调用输入和验收证据。`77b266e8` 已收紧 invocation evidence，invalid / unavailable / duplicate / collision 不能进入顶层已发布证据。`4fec7fab` 新增 `nativeSearchReleaseHandoffPlan`：当前 publish-target / source ready 时只进入 `ready_for_release_handoff` 并输出候选 publish 命令；该字段的 `doesNotVerify` 仍明确不证明 published / installed / packaging config / packaged binary / default enable，且 ready 字段必须绑定 `optionalPackageExecutionPlan.nextStage`，后置 packaged smoke 布尔值不能跳过上游 publication gate。

`3f5a0783` 新增 `nativeSearchReleaseHandoffPlan.gateBinding` 和 `packagedBundledBinarySmokeInvocationPlan.executionDesign`：前者显式记录 authoritative next gate 必须来自 `optionalPackageExecutionPlan.nextStage`，verified 必须要求 execution plan verified，乱序 publication / packaged smoke evidence fail closed；后者只记录真实 packaged smoke 的 preferred packaged-app-root、legacy node_modules root、registry check、real packaged app 和禁止临时 fixture evidence 等设计约束，不创建 packaged app、不发布、不安装、不修改 builder、不证明 bundled binary。

`ddc818ae` 新增 `packagingConfigAllowlistChangePlan.candidateYamlEditPlan`：只读记录后续真实 builder allowlist 修改的 YAML edit intent，包括 `targetFile="apps/electron/electron-builder.yml"`、`modifiesFile=false`、操作顺序、待添加精确 per-package include、需要移除的阻断规则、禁止宽泛 include 和验收证据。该字段不修改 builder、不发布、不安装、不读取 binary、不证明 `packagingConfigVerified`、不证明 packaged binary，也不允许把 dry-run / edit intent 当成 default-enable 证据。

`99d76b0c` 新增 `nativeSearchReleaseHandoffPlan.currentGateApprovalPacket`：只读记录当前顺序 gate 的批准准备包，包括 gate、status、requiredApproval、requiredEvidenceBeforeExecution、allowedActionsAfterApproval、candidateCommands、doesNotAuthorize 和 forbiddenActions。blocked gate 不输出可执行命令；当 publish-target / source preflight ready 且 publication invocation ready 时，packet 只进入 `ready_for_approval` 并要求 `release_approval`，只能授权候选 publish 调用审核，不授权 package.json / bun.lock / builder / packaged app / default enable。该字段不查询 registry、不发布、不安装、不写 manifest / lockfile / builder、不读取 binary、不证明 packaged binary。

`197569d5` 新增 `nativeSearchReleaseHandoffPlan.gateApprovalQueue`：只读记录后续真实 gate 的顺序批准矩阵，覆盖 optional package publication、optionalDependencies declaration、install-chain、builder allowlist、packaged app bundled binary smoke 和 default-enable risk review。只有当前 ready gate 暴露候选命令；未来 gate 必须保持 `blocked_until_prior_gate_verified` 且 `candidateCommands=[]`；已完成 gate 只能标为 `verified`，不能授权重复执行。该队列不发布、不安装、不写 manifest / lockfile / builder、不创建 packaged app、不读取 binary、不证明 packaged binary 或 default enable。

`cb30b819` 新增 `nativeSearchReleaseHandoffPlan.gateExecutionEvidenceChecklist`：只读记录每个真实 gate 在批准执行后必须补齐的 evidence、post-execution verification command、文件 / 网络 / 远端写副作用标记、success criteria、fail-closed criteria、doesNotVerify 和 forbiddenActions。默认 blocked gate 不暴露执行命令；publication ready 时只有当前 `optional_package_publication` gate 暴露批准后的 registry verification command。该清单不发布、不安装、不写 manifest / lockfile / builder、不创建 packaged app、不读取 binary、不证明 packaged binary 或 default enable。

`72a00b01` 修正 `gateExecutionEvidenceChecklist` 的 default-enable risk review 网络副作用边界：该 gate 后续验证设计包含显式 `--check-registry`，因此 `networkRequired=true`，但 blocked future gate 继续保持 `postExecutionVerificationCommands=[]`，不授权提前联网执行。

`b874628b` 新增 `gateExecutionEvidenceChecklist` 的执行后状态迁移期望：每个 gate 现在输出 `expectedNextGateAfterVerification`、`expectedSummaryFlagsAfterExecution`、`mustRemainUnverifiedAfterExecution` 和 `transitionFailClosedCriteria`。publication gate 验证后只应推进到 optionalDependencies declaration；declaration gate 验证后只应推进到 install-chain，且 lockfile / installed package 子证据仍必须保持未验证；后续 gate 也必须按真实顺序推进。该字段只做真实 gate 执行后的验收输入，不驱动 `nextGate`、`verified`、候选命令或 default-enable。

`66da7eba` 新增 `evaluateNativeSearchReleaseHandoffGateTransition()`：该纯函数把 `gateExecutionEvidenceChecklist` 的 transition expectation 转成 before / after summary 校验，要求当前 gate 已 ready，执行后只到达 expected next gate，expected summary flags 成立，must-remain-unverified flags 仍未验证，并输出 missing flags、violated flags、no-go boundary violations 和 failed criteria。它覆盖 publication、declaration、install-chain、packaging config 和 packaged smoke 的合规迁移，也拒绝跳 gate、提前证明 lockfile / installed package / packaged binary、以及 blocked packaged smoke 只靠顶层布尔值绕过。它不查询 registry、不发布、不安装、不写 manifest / lockfile / builder、不创建 packaged app、不读取 binary、不证明 packaged verified 或 default-enable。

`461295b0` 新增 `nativeSearchReleaseHandoffPlan.currentGateExecutionInputPacket`：该 packet 只读绑定 current gate approval packet 与 execution evidence checklist，输出当前顺序 gate 的批准前输入、批准后允许动作、候选命令、执行后验证命令、副作用边界、required evidence、expected next gate、must-remain-unverified、doesNotVerify 和 forbiddenActions。blocked / future gate 不暴露命令；declaration ready 不暴露 install；install-chain ready 只有批准后才暴露 `bun install --frozen-lockfile`；packaged smoke 只暴露 `<packaged-app-root>` 占位命令；default-enable 在当前 smoke 中仍 blocked。该 packet 不执行发布、不安装、不写 package manifest / lockfile / builder、不创建 packaged app、不读取 binary、不证明 packaged verified 或 default-enable。

`8a0bcfd5` 新增 `evaluateNativeSearchReleaseHandoffNoGoBoundary()`：该只读 audit 校验 smoke summary 的 default off / explicit opt-in、release handoff default-off 绑定、approval packet / execution input packet / approval queue / execution checklist 的 command 边界、install-chain 副作用、packaged smoke `<packaged-app-root>` 占位命令、default-enable blocked、提前 verified flag 和敏感路径 / registry 字符串泄露。blocked approval packet 不要求 ready command；只有 ready packet 才比较 expected command set。该 audit 不联网、不执行 `npm publish`、不运行 install、不写 package manifest / lockfile / builder、不创建 packaged app、不读取 binary，不证明 packaged verified 或 default-enable。

`63e0fc2b` 将上述 no-go audit 外显为 `nativeSearchReleaseHandoffNoGoBoundaryAudit` summary 字段和 `release-handoff-no-go-boundary-audit` smoke case；case failed 会让 smoke CLI fail closed。该提交还要求 installed optional package manifest 通过 optional package source package.json 严格 shape 校验，拒绝只有 `name/version`、lifecycle scripts 或 runtime dependencies 的 installed package evidence。它仍不发布、不安装、不新增真实 optionalDependencies、不修改 builder、不创建 packaged app / binary，也不证明 packaged verified 或 default-enable。

`9e3e3dff` 新增 optional package publish-target dry-run；`dc75f380` 后显式 `--check-registry --native-search-package-version 0.0.3` 当前 4 个 planned package 目标版本可发布、无 registry collision，且 source version consistency 已通过，`optionalPackagePublishTargetReady=true`。`05c393bb` 后 `optional-package-source --native-search-package-version 0.0.3` 输出 `optionalPackageSourceReady=true`，只说明 planned source metadata 形状 ready。`ce89f490` 后 `optionalPackageExecutionPlan.completedPrerequisites` 是严格顺序前缀，`observedEvidenceStages` 只表示独立 evidence；summary 顶层 `optionalPackagePublishTargetReady` / `optionalPackageSourceReady` 也必须绑定 checked，避免未检查状态被误判为 ready。`a3f98673` 后 builder allowlist dry-run plan 只输出实际修改前 review 输入，不会把 `packagingConfigVerified` 或 `readyForPackagingConfigChange` 误判为真实通过；`ddc818ae` 后 `candidateYamlEditPlan` 也只表示后续 YAML edit intent，不会把 `modifiesFile=false` 的计划当成 builder 已修改或 allowlist 已 verified。`99d76b0c` 后 `currentGateApprovalPacket` 只表示当前 gate 的批准准备，不会把 `ready_for_approval` 或 candidate command 当成真实 publish / install / builder / packaged / default-enable evidence；`197569d5` 后 `gateApprovalQueue` 只表示 gate 顺序和批准准备；`cb30b819` 后 `gateExecutionEvidenceChecklist` 只表示执行后证据清单和当前 gate 批准后验证命令，不表示执行已发生；`72a00b01` 后 `networkRequired` 只表示未来 gate 执行验证的副作用边界，不授权 future gate 提前联网；`b874628b` 后 transition expectation 只表示执行后应验收的 summary 迁移，不表示执行已发生；`66da7eba` 后 transition verifier 只比较 before / after summary，不执行任何 gate，也不把 ready、candidate command、expected flag、top-level boolean 或 blocked packaged smoke plan 当作真实执行证据。`7726a008` 后 optionalDependencies install-chain dry-run plan 只输出声明 / 安装执行前 review 输入，不能把 `optionalDependenciesDeclared`、`optionalDependenciesInstallChainVerified`、`optionalDependenciesLockfileVerified`、`optionalDependenciesInstalledPackagesVerified` 或 packaged binary gate 误判为真实通过；`d679eabb` 后 optional package publication change plan 只输出真实发布前 review 输入，`publishedPackages` 只保留真实 registry publication evidence，`publishTargetCollisionPackages` 与 `existingInvalidPublishedPackages` 只用于阻断 / 排除命令；`edeba827` 后 packaged smoke invocation plan 只输出调用输入和验收证据，不会把 `appNodeModulesRootResolved`、`ready_for_execution` 或 candidate command 误判为真实 bundled binary；`f949fc13` 后 optional package publication invocation plan 只输出真实 `npm publish` 调用输入和候选命令，不会把 `ready_for_invocation` 或 candidate command 当作已发布；`77b266e8` 后 `publishedPackages` 只能来自 package-level `status=\"published\"` 且必须和 planned package set 唯一严格匹配；`4fec7fab` 后 release handoff ready 只表示当前顺序 gate 可交接，不代表真实远程写入 / install / builder / packaged / default-enable 已完成。`packaged-manifest` 与 `packaged-app-layout` 的 publication helper会绑定 planned source `BINARY_VERSION=0.0.3` 对应 exact expected version 到 registry `versions[expectedVersion]`，不能只看 latest。顶层 `realPackagedBinaryVerified` / `bundledBinaryVerified` 必须同时受非临时 fixture、optional package publication、optional install-chain、packaging config、packaged evidence、packaged identity 和真实 binary verification 约束，不能由临时 fixture、单项 resolver、publication-only、packaging-only、install-chain-only、publish-target dry-run、source preflight、execution plan、builder allowlist dry-run plan、builder YAML edit intent、handoff approval packet、gate approval queue、execution evidence checklist、transition expectation、transition verifier、optionalDependencies install-chain dry-run plan、optional package publication change plan、optional package publication invocation plan、packaged smoke invocation plan、release handoff plan 或宽泛 builder include 置 true。真实 optional package 发布、optionalDependencies 实际声明与安装执行、builder allowlist 实际修改、真实 packaged app bundled binary smoke 和最终 default-enable 风险决策仍未完成，native 仍不能默认启用。尚未完成：default enable、真实 optional package 发布、optionalDependencies 实际声明与安装执行、builder allowlist 实际修改、真实 packaged app bundled binary smoke、最终 default-enable 风险决策；Phase 6、Phase 7、Phase 9 已按 2026-06-07 当前范围暂停，Phase 8 全量打包/CI/发布收口不进入当前任务，仅保留 Phase 5 packaged smoke 所需最小 packaged 验证设计。

启动后先运行 `git status --short --branch` 和 `git log -25 --oneline`，确认最近历史包含 `885e89b5 docs(rust-go): 同步 Phase 5 no-go audit summary 状态`、`63e0fc2b feat(rust-go): 外显 no-go audit 并加固 install-chain manifest`、`345dcf01 docs(rust-go): 同步 Phase 5 no-go 边界审计状态`、`8a0bcfd5 feat(rust-go): 补齐 Phase 5 release handoff no-go 边界审计`、`7898dae6 docs(rust-go): 同步 Phase 5 执行输入证据包状态`、`461295b0 feat(rust-go): 补齐 Phase 5 release handoff 执行输入证据包`、`5be9c233 docs(rust-go): 回填 Phase 5 最新开发状态` 和 `66da7eba feat(rust-go): 补齐 Phase 5 gate 迁移校验器`。

下一步继续 Phase 5“Rust search sidecar 试点”的缩减计划，只保留 3 个核心 gate：1. 最小真实 optional package gate，先只处理当前可验证平台的 package source、发布和 registry evidence；2. 最小 install-chain gate，在真实 package evidence 通过后声明对应 optionalDependency、执行真实 install，并验证 `bun.lock`、installed package manifest metadata 和 native default off 不变；3. 最小 packaged smoke gate，在前两项通过后做精确 builder allowlist 修改，并验证一个真实 packaged app bundled binary 可从 app 内 `node_modules` 解析、可执行且不走系统 `PATH`。default-enable 继续暂缓；即使最小 packaged opt-in 通过，也只记录“packaged opt-in 可用”，不默认启用 native。不要进入 Phase 6、Phase 7 或 Phase 9；Phase 8 不做全量打包 / CI / 发布矩阵；Go supervisor 暂缓；不要新增 helper-only approval / handoff / dry-run 代码，除非真实 gate 执行暴露明确缺口。

当前 no-go：native 必须继续 default off / 显式 opt-in。默认 smoke 不联网。未获得明确批准前，不执行真实 `npm publish`，不运行真实 install，不创建 packaged native binary，不修改 `apps/electron/electron-builder.yml`，不修改根 `README.md` / 根 `AGENTS.md`，不新增真实 `@codeinsights/native-search-*` optionalDependencies，不 push，不创建 PR。

请遵守阶段纪律：每完成一个阶段并通过验证后，立即更新 development checklist、next-session-prompt.md、tasks/todo.md Review 和必要的 lessons，然后单独提交该阶段相关文件，提交信息使用详细中文。
```
